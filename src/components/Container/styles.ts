import { createStyles } from '@material-ui/core';

const styles = () =>
  createStyles({
    container: {
      width: '100%',
      maxWidth: 500,
      height: '100%',
      padding: 20,
      boxSizing: 'border-box',
      backgroundColor: 'red',
    },
  });

export default styles;
