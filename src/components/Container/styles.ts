import { createStyles } from '@material-ui/core';

const styles = () =>
  createStyles({
    container: {
      width: '100%',
      maxWidth: 500,
      height: '100%',
      backgroundColor: 'red',
    },
  });

export default styles;
