import { createStyles } from '@material-ui/core';

const styles = () =>
  createStyles({
    header: {
      height: 100,
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    },
    logo: {
      height: 80,
    },
  });

export default styles;
